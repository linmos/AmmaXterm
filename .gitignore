.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Local-only contributor/agent guide; not tracked.
CLAUDE.md

# Internal design docs (PRD / plan / spike notes); not tracked.
/docs/

# Updater signing keys — private key must never be committed (CI uses a repo
# secret); the public key already lives in tauri.conf.json, so the .pub is just
# a local backup.
*.key
*.key.pub
